# 순열, Permutation

- 서로 다른 n 개 중에서 r 개를 뽑아서 한 줄로 나열한 것
- 순서에 의미가 있음
  - `1-2-3` 이랑 `3-2-1` 은 다름
  - `nPr == n! / (n-r)! == nCr*r`
- N 개의 요소들에 대해 n! 개의 순열들이 존재
- ex) 한 줄로 나열하는 경우의 수

# 조합, Combination

- 서로 다른 n 개 중에서 r 개를 순서에 상관 없이 골라낸 것
- 순서가 의미 없음
  - `1-2-3` 이랑 `3-2-1` 이랑 같음
- ex) 로또
