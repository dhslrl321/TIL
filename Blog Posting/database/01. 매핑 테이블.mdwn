# 다대다 (N : M) 관계

두 개의 테이블이 서로의 행에 대해서 여러 개로 연관되어 있는 상태를 N : M 관계라고 한다.

예를 들어 **한 명이 여러 옷을 주문할 수 있고 하나의 옷은 여러 사람들에거 주문될 수 있을 떄** N:M 관계라고 한다.

# 매핑 테이블

위와 같은 N:M 관계는 논리적으로 구현은 가능하나 2개의 물리적 테이블로는 구현이 불가능 하다.
다대다 관게를 실제로 구현하려면 각 테이믈의 PK를 FK로 참조하는 매핑 테이블이 있어야 한다.

그래서 N 관계인 회원 테이블이 `user_order` 테이블에서 pk를 fk로 참조하며 옷 테이블이 `user_order` 테이블에서 PK를 fk로 참조되는 새로운 테이블이 생성된다.
