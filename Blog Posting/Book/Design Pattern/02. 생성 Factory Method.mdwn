# 생성 패턴

- 객체의 생성과 관련된 패턴
- 객체의 생성과 조합을 캡슐화 하여, 객체가 생성되거나 변경되어도 프로그램 구조에 영향을 받지 않도록 유연성을 제공하는 것이 목적

# 팩토리 메서드 Factory Method 패턴

- 객체를 생성하는 인터페이스를 정의하나 인스턴스를 만들 클래스의 결정은 서브 클래스가 하도록 위임, 즉 `new` 키워드를 호출하는 부분은 서브 클래스가 담당
- 객체 생성을 캡슐화 하는 패턴
- 이렇게 하면 어떤 객체의 인스턴스를 생성할지를 서브 클래스에서 결정할 수 있음
- Creator 의 서브 클래스에 Factory Method를 정의하고 Factory Method 의 호출로 적절한 ConcreteProduct 인스턴스를 반환

# 참여 객체

- Creator :
- ConcreteCreator
- Product
- ConcreteProduct

# 예시

# 장점과 단점

### 장점

-

### 단점
