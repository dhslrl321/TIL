# 구현

- 머리속에 있는 알고리즘을 소스코드로 바꾸는 과정.
- 사실상 모든 알고리즘 문제에서는 **구현의 과정이 필수**라 자주 다루지 않는데, 중요함
- 보통 문제로 출제되면, **생각하기는 쉬운데, 코드로 옮기는 것이 까다로운 문제**들이 출제된다.
  - 흔히 알고리즘 PS 에서 피지컬이라고 하는 분야로 타자나 프로그래밍 문법에 빠삭하고 잘 아는 사람들이 유리함
  - 예를 들어서, 문자열 단위로 파싱해서 리스트에 넣기, 특정 소수점 자리까지 출력과 같은 까다로운 문제들
- **완전 탐색이나 시뮬레이션**이 구현에 속함
  - 완전 탐색 : 모든 경우의 수를 주저 없이 다 계산하는 방법
  - 시뮬레이션 : 문제에서 제시한 알고리즘을 한 단계씩 수행

# 문제 접근

- 문제 길이가 길지만 고차원적인 사고를 요하지는 않음
