### 목차

- 배경
- how 를 표현하는것과 what 을 표현하는 것의 차이
- Spring Data 모듈의 findById

# 배경

시간은 조금 지났지만 최근 **조영호님의 오브젝트라는 책**을 주제로 스터디를 진행하고 있었다.

해당 책에서 이런 말이 나온다

#### 메서드는 how 보다 what 을 표현해야 한다

이 글을 본 순간 우리 스터디원들은 모두가 하나같이 하는 말이 있었다

> 응? 그럼 Spring Data JPA 나 Data JDBC 같은 모듈에서 말하는 `findById()` 같은 메서드는 뭐지?

# 메서드는 how 보다 what 을 표현해야 한다

예를 들어 다음과 같은 클래스 하나가 존재한다고 가정해보자

```java
public class NumberGenerator {

}
```
