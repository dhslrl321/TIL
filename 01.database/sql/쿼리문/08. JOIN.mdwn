# 조인

지금까지 배운 모든 쿼리는 **하나의 테이블**에서 동작하는 명령이었다.
이제부터는 두 개 이상의 테이블을 묶어서 하나의 결과 집합을 만들어 내는 **조인**에 대해서 배워보자.

- 조인은 주로 **매핑 테이블**에서 SQL을 쓸 때 사용된다.
- 1:n으로 연결된 매핑 테이블을 기준으로 두 테이블을 조인해서 결과를 추출한다.
- 이런 조인에는 2가지 방뻡이 존재한다.

## INNER JOIN

내부 조인이라고 불리는 INNER JOIN은 거의 대부분의 업무에서 사용된다.
