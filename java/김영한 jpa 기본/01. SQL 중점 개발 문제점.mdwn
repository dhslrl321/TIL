# 데이터베이스

- 현재는 관계형 DB가 점령하고 있음
- 객체를 관계형 DB에 관리

# 객체와 관계형 DB의 차이

- 상속
  - 객체의 상속 관계를 DB에서는 표현할 수 없으니 슈퍼타입과 서브타입 관계로 표현
- 연관 관계
  - 객체의 참조 : getTeam()
  - 테이블의 참조 : 외래 키 사용
- 데이터 타입
- 데이터 식별 방법

### 결국 생산성을 위해서 JPA를 사용함
