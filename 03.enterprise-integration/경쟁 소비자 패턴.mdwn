# 경쟁 소비자 패턴, competing-consumers

- 여러 동시 소비자가 동일한 메시징 채널에 수신된 메시지를 처리할 수 있게 한다
- 여러 동시 소비자를 통해서 시스템은 여러 메시지를 동시에 처리한다
  - 처리량 최적화
  - 확장성 및 가용성 개선
  - 워크로드 균형

# 문제점

- 요즘 시대의 애플리케이션은 많은 요청을 처리해야 함
- 일반적으로는 각 요청을 동기적으로 처리하지만 경쟁 소비자 패턴에서는 메시징 시스템을 이용해서 비동기적으로 처리하는 다른 서비스로 전달함
