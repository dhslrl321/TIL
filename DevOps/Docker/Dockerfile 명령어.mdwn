## DockerFile 명령어

`FROM` ==> 생성할 이미지의 베이스가 될 이미지 (필수)로 사용할 이미지가 없으면 자동으로 pull
`LABEL` ==> 이미지에 메타데이터 추가로 key:value 형태로 지정
`RUN` ==> 이미지를 만들기 위해 컨테이너 내부에서 명령어를 실행
`ADD` ==> 파일을 이미지에 추가
`WORKDIR` ==> 명령어를 실행할 디렉터리를 나타냄
`EXPOSE` ==> 이미지에서 노출할 포트이지만 반드시 호스트 포트랑 포워딩되는 것은 아니고 단지 컨테이너의 해당 포트만 사용하겠다는 소리
`CMD` ==> 컨테이너가 실행될 때 마다 커멘드를 설정하고 한 번만 사용 가능
